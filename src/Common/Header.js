import * as React from 'react';
import { View, Text } from 'react-native';

function Search() {
  return (
    <View style={{flex:1}}>
      <View style={{ width: '100%', height: 70, backgroundColor: '#fff', position: 'absolute', top: 0, flexDirection: 'row', alignItems: 'center' }}>
        <Text>atas</Text>
      </View>
    </View>
  );
}

export default Search;
